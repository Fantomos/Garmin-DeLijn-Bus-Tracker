# Garmin DeLijn Bus Tracker
Check the next bus timing based on the realtime API provided by DeLijn.
A one time setup is required to fill the API key and favorites bus stop.
For all DeLijn bus (Belgium).
